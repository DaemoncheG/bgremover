# 🧪 Автотесты: роадмап покрытия сценариев

Цель — покрыть **основные пользовательские сценарии** CLI и пайплайна без
тяжёлых зависимостей и больших медиафайлов в репозитории.

## 1) Базовые CLI сценарии
- [ ] `--help` и отсутствие аргументов → корректный вывод usage.
- [ ] `--list-models` → успешный код завершения и список моделей.
- [ ] Ошибки ввода: несуществующий input, output совпадает с input.

## 2) Автовыход и валидации CLI (новые UX-правки)
- [ ] `input.jpg` без `output` → создаётся `input_out.png` рядом.
- [ ] `input/` без `output` → создаётся `input_out/`.
- [ ] `--img-quality` вне диапазона → предупреждение и дефолтное значение.
- [ ] `--bg-color` неизвестный → предупреждение + fallback на black.
- [ ] `--gif-fps-limit` и `fps==0` → корректная обработка.

## 3) Изображения (happy-path)
- [ ] PNG → PNG (прозрачность сохранена).
- [ ] PNG → JPG (фон белый).
- [ ] JPG → WebP.
- [ ] EXIF rotate (проверка корректной ориентации).

## 4) Видео (минимальные интеграции)
- [ ] MP4 → WebM (прозрачность).
- [ ] MP4 → MP4 с цветным фоном (`--bg-color`).
- [ ] MP4 → GIF (fps limit).
- [ ] Проверка переноса аудио (`transfer_audio`).

## 5) Папки и batch-режим
- [ ] Входная папка с подструктурой → структура сохраняется в output.
- [ ] Ошибка, если output-папка внутри input.

## 6) Производительность и стабильность
- [ ] Многопроцессность: `--coreml/--cpu` корректно суммируются.
- [ ] Большие входы: нет утечек/роста памяти (smoke test).

## 7) Договорённости по среде тестов
- [ ] `opencv-python-headless` в зависимостях тестов.
- [ ] `ffmpeg` обязателен только для видео-тестов.
- [ ] Видео-тесты помечены `@pytest.mark.slow`.
