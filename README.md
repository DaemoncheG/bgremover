# 🚀 AI Background Remover (RemBG Pipeline)

Инструмент для автоматического удаления фона с **изображений** и **видео**.
Написан на Python, использует rembg (модели U2Net/ISNet/BiRefNet/BRIA) и обработку через FFmpeg.

## ✨ Возможности

- 🍏 Оптимизация под Apple Silicon (CoreML, Neural Engine)
- 🖼 Удаление фона с изображений
- 🎥 Обработка видео (mp4/webm/gif + поддержка входных mov/avi/mkv)
- 🔊 Сохранение оригинальной аудиодорожки
- 📁 Пакетная обработка папок с сохранением структуры
- 🛡 Защита от перезаписи исходных файлов

---

## 🛠 Установка

### 1. FFmpeg (обязательно)

**macOS**
```bash
brew install ffmpeg
```

**Windows**
Скачайте с сайта ffmpeg.org и добавьте путь к bin в PATH

**Linux**
```bash
sudo apt install ffmpeg
```

---

### 2. Установка проекта

**Требования**
- Python 3.9+ (рекомендуется 3.10+)
- FFmpeg установлен и доступен в `PATH`
- Ключевые зависимости: `rembg`, `onnxruntime`, `opencv-python`, `Pillow`

```bash
git clone https://github.com/DaemoncheG/bgremover.git
cd bgremover
```

Создание виртуального окружения:

```bash
# macOS / Linux
python3 -m venv venv
source venv/bin/activate

# Windows
python -m venv venv
venv\Scripts\activate
```

Установка зависимостей:

```bash
pip install -r requirements.txt
```

---

## 🚀 Использование

### Синтаксис CLI

```bash
python main.py <input> <output> [options]
```

`input` может быть файлом или папкой. `output` — файл или папка.

> Важно: фактический формат вывода **всегда** задаётся через `--img-format` и `--vid-format`
> (по умолчанию: `png` и `webm`). Если указать `output.jpg`, но оставить `--img-format png`,
> файл будет сохранён как PNG с переименованием.

### Quickstart (изображения)

**1) Самый простой вариант**
```bash
# Входная картинка -> прозрачный PNG
python main.py input.jpg output.png
```

**2) Явно указать модель**
```bash
python main.py input.jpg output.png --model u2net
```

**3) Сохранить результат в JPG (фон станет белым)**
```bash
python main.py input.png output.jpg --img-format jpg
```

**4) Сохранить результат в WebP**
```bash
python main.py input.jpg output.webp --img-format webp
```

**5) Папка целиком (все картинки внутри)**
```bash
# Все изображения из папки input/ попадут в output/ с сохранением структуры
python main.py input/ output/
```

**6) Папка с точкой в названии (обязательно слэш в конце)**
```bash
# Если у папки есть точка, добавьте слэш, чтобы не спутать с расширением
python main.py photos.v1/ out/
```

**7) Если хотите, чтобы выход был всегда PNG**
```bash
python main.py input.jpg output.png --img-format png
```

### Quickstart (видео)

**1) Прозрачный фон (рекомендуется)**
```bash
# Лучший вариант для прозрачности — WebM
python main.py video.mp4 result.webm
```

**2) MP4 с заданным фоном**
```bash
python main.py video.mp4 result.mp4 --vid-format mp4 --bg-color black
```

**3) MP4 с зеленым фоном (для хромакея)**
```bash
python main.py video.mp4 result.mp4 --vid-format mp4 --bg-color green
```

**4) GIF**
```bash
python main.py video.mp4 result.gif --vid-format gif
```

**5) Пакетная обработка папки с видео**
```bash
python main.py videos/ out_videos/
```

**6) Ускорение (для macOS Apple Silicon)**
```bash
# Использовать CoreML (обычно быстрее)
python main.py video.mp4 result.webm --coreml 2
```

**7) Ускорение (CPU, Windows/Linux)**
```bash
python main.py video.mp4 result.webm --cpu 2
```

---

## ⚙️ Параметры

**Позиционные аргументы**
- `input` путь к файлу/папке (изображение или видео)
- `output` путь к файлу/папке для результата

**Опции**
- `-m`, `--model` выбор модели сегментации  
  - u2net (по умолчанию)
  - u2netp
  - u2net_human_seg
  - u2net_cloth_seg
  - silueta
  - isnet-general-use
  - isnet-anime
  - sam
  - birefnet-general
  - birefnet-general-lite
  - birefnet-portrait
  - birefnet-dis
  - birefnet-hrsod
  - birefnet-cod
  - birefnet-massive
  - bria-rmbg

- `--img-format` формат вывода изображений: `png`, `webp`, `jpg` (по умолчанию `png`)
- `--vid-format` формат вывода видео: `webm`, `mp4`, `gif` (по умолчанию `webm`)
- `--bg-color` цвет фона (black, white, green, blue). Любое другое значение трактуется как `black`.
- `--coreml` число процессов CoreML (по умолчанию 2 на Apple Silicon). При ошибках CoreML автоматически используется CPU.
  - Рекомендуемые значения: 1–4
- `--cpu` число CPU процессов (по умолчанию 2 на Windows/Linux)
  - Рекомендуемые значения: 1–4
- `--verbose` подробный вывод FFmpeg и ошибок ускорителей
- `--list-models` вывести список моделей и завершить работу

Список моделей:

```bash
python main.py --list-models
```

### Форматы входа/выхода

**Изображения (вход):** `.png, .webp, .jpg, .jpeg, .bmp, .tiff`  
**Видео (вход):** `.mp4, .mov, .avi, .gif, .mkv, .webm`  
**Выходные форматы:** через `--img-format` / `--vid-format` (см. выше).

### Когда какую модель выбирать

- **u2net** — универсальная модель для большинства сцен и предметов.
- **u2netp** — легкая и быстрая, подходит для слабых CPU и пакетной обработки.
- **u2net_human_seg** — сегментация человека, хороший выбор для портретов и людей в кадре.
- **u2net_cloth_seg** — выделение одежды (верх/низ/полный образ), полезно для fashion-каталогов.
- **silueta** — уменьшенный u2net, компромисс скорость/качество для массовой обработки.
- **isnet-general-use** — современная универсальная модель с хорошими контурами объектов.
- **isnet-anime** — специализирована под аниме/иллюстрации.
- **sam** — универсальная сегментация, лучше работает с четкими объектами; для точных сценариев обычно нужны промпты (в этой утилите промпты не настраиваются).
- **birefnet-general** — сильная универсальная модель для сложных границ и деталей.
- **birefnet-general-lite** — облегченная версия birefnet-general для ускорения.
- **birefnet-portrait** — оптимизирована для портретов и людей.
- **birefnet-dis** — для задач бинарной сегментации (salient object/DIS).
- **birefnet-hrsod** — для высокодетальных сцен и объектов высокого разрешения.
- **birefnet-cod** — для скрытых/малозаметных объектов (concealed object detection).
- **birefnet-massive** — обучена на большом датасете, подходит для смешанных сценариев.
- **bria-rmbg** — современная модель с высоким качеством, особенно на сложных фотографиях.

---

## 💻 Совместимость

- macOS Apple Silicon - CoreML
- Windows - CPU
- Linux - CPU

### Видео и прозрачность

- Для прозрачного фона используйте `webm` (поддержка alpha-канала).
- Для `mp4` задайте цвет фона через `--bg-color`.
- Для `mp4` требуется FFmpeg с `libx264`. Если кодек недоступен, MP4-вывод будет пропущен.

---

## ✅ Тестирование

Быстрый smoke-тест CLI (скрипт находится в `scripts/smoke_test.sh`):

```bash
./scripts/smoke_test.sh
```

Автотесты (pytest):

```bash
pytest
```

Если нужны только быстрые тесты без интеграционных:

```bash
pytest -m "not slow"
```
